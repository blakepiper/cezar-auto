# Superseded: Rust workspace decision

This record belonged to the retired TypeScript service tree. The shipped implementation is now a
Rust workspace under `crates/`, with the terminal binary built from
`crates/coducktor-tui`. The active build and compatibility rules are documented in `AGENTS.md`,
`AGENT_PROTOCOL.md` and `BACKWARD_COMPATIBILITY.md`.

The file remains as an ADR index entry so links from old planning records do not disappear; it no
longer defines a dependency or toolchain decision for the product.
